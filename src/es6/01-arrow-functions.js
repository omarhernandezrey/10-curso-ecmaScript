// Declaración de función estándar
function square(num) {
    return num * num;
}

// Expresión de función flecha
const square = (num) => {
    return num * num;
}

// Expresión de función flecha compacta
const square = num => num * num;
